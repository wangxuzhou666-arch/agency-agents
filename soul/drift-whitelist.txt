# SOUL drift whitelist — exemptions for blacklist terms / missing paths that are intentional.
# Format (one per line, exact string match):
#   BLACKLIST:<exact term>     — suppress a blacklist hit
#   PATH:<exact path>          — suppress a missing-path hit
#
# Example:
#   BLACKLIST:VC 三问
#   PATH:scripts/legacy/old.md

# Descriptive placeholder paths (not real file refs)
PATH:/.claude/projects/.../memory/MEMORY.md
# Descriptive nouns mentioned in prose (not real file refs)
PATH:settings.json

# VC 模型 framework spec — 实际在私有 ~/Desktop/colar-memory/frameworks/vc-model/spec/v0.6.md
PATH:spec/v0.6.md
